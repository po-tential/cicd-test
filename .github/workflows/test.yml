name: Manual Trigger

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Enter a custom message'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "${{ github.event.inputs.message }}"